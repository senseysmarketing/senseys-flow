

## Ajuste do Badge de Origem

Mudança em uma linha de `LeadKanbanCard.tsx`:

- Linha 146: Alterar `showLabel={false}` para `showLabel={true}` no `OriginBadge`, para que ele exiba ícone + texto (ex: "Facebook") assim como o `TemperatureBadge` exibe "Quente 🔥".

Isso garante que ambos os badges tenham a mesma proporção, tamanho e alinhamento visual.

